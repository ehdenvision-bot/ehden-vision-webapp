# TODO

# Pending — highest value first

## 🔴 Decide the target stack for the full rewrite
Blocks a real answer to the open decision in `agents/decisions.md`, and the placeholders in
`CLAUDE.md`'s "Repo Management" section. Not urgent while `Webapp Files/` is still the only thing
being worked on, but worth deciding deliberately rather than by default.

*(Add real items here as they come up. This list should track `agents/decisions.md`'s open
decisions and whatever's actually next — don't pad it out for its own sake.)*

# Backlog / Someday

*(Empty for now.)*