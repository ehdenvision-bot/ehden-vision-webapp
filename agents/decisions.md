# Decisions Log

Resolved decisions move to the archive section at the bottom.

---

## Open Decisions

### Target stack for the full rewrite not yet chosen
The live app is Google Apps Script (`Webapp Files/`), tested against Google Workspace. The plan is to
migrate to a different stack later, but it hasn't been picked yet. **[Fill in once decided — the
choice affects `CLAUDE.md`'s "Repo Management" section, `agents/current-state.md`, and where the
new codebase will live.]**

---

## Resolved Decisions

*(None yet. Once a real tradeoff gets decided — architecture, a data-model shape, a scoping cut
— write down what was considered, what was chosen, and why, dated. Move the entry down from
"Open Decisions" above once it's actually resolved; don't just delete the open one.)*