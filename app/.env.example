NODE_ENV=development
PORT=3000

DATABASE_URL="postgresql://chantier:chantier@localhost:5432/chantier?schema=public"

JWT_SECRET=change-me-dev-secret
JWT_EXPIRES_IN=7d

UPLOAD_DIR=./uploads

# SMTP (replaces Gmail-send scopes used by the Apps Script version)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Chantier App <no-reply@example.com>"
